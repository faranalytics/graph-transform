{
  "name": "graph-transform",
  "version": "0.0.5",
  "description": "",
  "main": "./dist/index.js",
  "scripts": {
    "test": "cd ./tests/test && npm install && npm run clean:build && npm start",
    "clean": "rm -rf ./dist/*",
    "build": "tsc --build .",
    "clean:build": "npm run clean && npm run build",
    "watch": "tsc --watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/faranalytics/graph-transform.git"
  },
  "author": "FAR",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/faranalytics/graph-transform/issues"
  },
  "homepage": "https://github.com/faranalytics/graph-transform#readme",
  "engines": {
    "npm": ">=10.2.3",
    "node": ">=18.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.28",
    "typescript": "^5.4.5"
  },
  "keywords": [
    "graph",
    "stream",
    "transform",
    "graph-transform"
  ]
}
